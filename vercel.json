{
  "outputDirectory": "dist",
  "installCommand": "npm install --no-package-lock --include=dev",
  "buildCommand": "npx vite build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}